"""Trade system package for RugbyDraft.

Modules:
    window         — trade window open/closed check (pure)
    validate_trade — 7-rule proposal validation (pure)
    processor      — state machine: propose/accept/reject/cancel/veto (pure)
"""
