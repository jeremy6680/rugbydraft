# connectors/requirements.txt
# Connector dependencies — independent of the backend (FastAPI).
# The connectors package is used by both the pipeline scripts and CI.

# Pydantic v2 — used by BaseRugbyConnector models
pydantic==2.11.1

# HTTP client — used by DSGConnector (HTTP Basic Auth + XML fetch)
httpx==0.28.1