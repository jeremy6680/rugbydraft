# dbt_project/requirements.txt
# Pipeline dependencies — pinned for reproducible CI runs.
# Install with: pip install -r dbt_project/requirements.txt

# dbt-duckdb includes dbt-core==1.9.x as a transitive dependency
dbt-duckdb==1.9.2

# dbt-postgres must match dbt-core minor version (1.9.x)
# Required for gold models running against Supabase PostgreSQL (D-030)
dbt-postgres==1.9.1

# Note: dbt-utils is a dbt package (not a pip package).
# Installed via: dbt deps (reads dbt_project/packages.yml)